import { RespondDirective } from './respond.directive';

describe('RespondDirective', () => {
  it('should create an instance', () => {
    const directive = new RespondDirective();
    expect(directive).toBeTruthy();
  });
});
